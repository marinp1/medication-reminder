import { IFailure } from 'better-validator/src/Base';
import * as format from 'better-validator/src/format';
import { IsAnything } from './IsAnything';
import { ExpressMiddleware } from 'better-validator/src/middleware/ExpressMiddleware';
import { Koa2Middleware } from 'better-validator/src/middleware/Koa2Middleware';
import { KoaMiddleware } from 'better-validator/src/middleware/KoaMiddleware';
declare class Validator {
  public static default: typeof Validator;
  public static readonly format: typeof format;
  public static expressMiddleware(options: any): ExpressMiddleware;
  public static koaMiddleware(options: any): KoaMiddleware;
  public static koa2Middleware(options: any): Koa2Middleware;
  public static create(
    options?: any,
  ): ((value: any) => IsAnything) &
    ({
      run: () => IFailure[];
    });
  constructor(options: any);
}
export = Validator;
